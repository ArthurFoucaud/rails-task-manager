<h1>My Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
  <li><input type="checkbox" <%= "checked" if task.completed  %> >
<%= link_to task.title, task_path(task) %></li>
  <% end %>
  <%= link_to "Create", tasks_new_path%>
</ul>
